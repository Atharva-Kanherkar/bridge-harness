{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "ExternalImportConflictPolicy": {
      "enum": [
        "skip",
        "import_as_new_historical_revision",
        "keep_existing",
        "import_alongside"
      ],
      "type": "string"
    },
    "ExternalImportPlan": {
      "properties": {
        "conflictPolicy": {
          "$ref": "#/definitions/ExternalImportConflictPolicy"
        },
        "createdAt": {
          "type": "string"
        },
        "dryRun": {
          "type": "boolean"
        },
        "memoryScope": {
          "type": [
            "string",
            "null"
          ]
        },
        "selectedCandidateIds": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "setupActivationPolicy": {
          "$ref": "#/definitions/ExternalImportSetupActivationPolicy"
        }
      },
      "required": [
        "conflictPolicy",
        "createdAt",
        "dryRun",
        "selectedCandidateIds",
        "setupActivationPolicy"
      ],
      "type": "object"
    },
    "ExternalImportSetupActivationPolicy": {
      "enum": [
        "disabled"
      ],
      "type": "string"
    }
  },
  "properties": {
    "discoveryId": {
      "description": "Same discovery `preview` reviewed. Commit re-derives every candidate from disk against it rather than trusting client-supplied `normalizedPayload`/`contentHash`/`candidateId` values — the source file is re-read and re-hashed at commit time, closing the discover/preview/commit TOCTOU window.",
      "type": "string"
    },
    "plan": {
      "$ref": "#/definitions/ExternalImportPlan"
    }
  },
  "required": [
    "discoveryId",
    "plan"
  ],
  "title": "CommitExternalImportParams",
  "type": "object"
}
