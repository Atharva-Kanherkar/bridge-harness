{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "AgentShortcutDisposition": {
      "enum": [
        "launched",
        "queued",
        "awaitingApproval"
      ],
      "type": "string"
    },
    "SecretInterception": {
      "description": "Mirrors `bridge_core::secret_interception::SecretInterception` — one secret replaced by a broker reference before the turn left the machine.",
      "properties": {
        "detector": {
          "type": "string"
        },
        "reference": {
          "description": "The broker reference substituted into the text; the secret itself never crosses the wire.",
          "type": "string"
        }
      },
      "required": [
        "detector",
        "reference"
      ],
      "type": "object"
    }
  },
  "description": "The reservation outcome the composer can render without asking an orchestrator to interpret a worker lifecycle event.",
  "properties": {
    "agentId": {
      "type": "string"
    },
    "agentName": {
      "type": "string"
    },
    "childSessionId": {
      "type": [
        "string",
        "null"
      ]
    },
    "disposition": {
      "$ref": "#/definitions/AgentShortcutDisposition"
    },
    "interceptions": {
      "items": {
        "$ref": "#/definitions/SecretInterception"
      },
      "type": "array"
    },
    "role": {
      "type": "string"
    }
  },
  "required": [
    "agentId",
    "agentName",
    "disposition",
    "interceptions",
    "role"
  ],
  "title": "DispatchAgentShortcutResult",
  "type": "object"
}
