{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "JsonRpcVersion": {
      "const": "2.0",
      "type": "string"
    },
    "Params": {
      "description": "Structured parameters: an object or an array, per JSON-RPC 2.0.",
      "type": [
        "object",
        "array"
      ]
    },
    "RequestId": {
      "anyOf": [
        {
          "maximum": 9007199254740991.0,
          "minimum": -9007199254740991.0,
          "type": "integer"
        },
        {
          "type": "string"
        }
      ],
      "description": "A request identifier. Clients choose; servers echo it back verbatim. Numeric ids must stay within JavaScript's safe integer range."
    }
  },
  "properties": {
    "id": {
      "$ref": "#/definitions/RequestId"
    },
    "jsonrpc": {
      "$ref": "#/definitions/JsonRpcVersion"
    },
    "method": {
      "description": "A method from the registry, `protocol/handshake`, or `$/cancel`.",
      "type": "string"
    },
    "params": {
      "$ref": "#/definitions/Params"
    }
  },
  "required": [
    "id",
    "jsonrpc",
    "method"
  ],
  "title": "RpcRequest",
  "type": "object"
}
