{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "WorkBriefReceiptOutcome": {
      "description": "What a trigger got: a run it started, a run somebody else already holds, or a refusal with a stable code.",
      "oneOf": [
        {
          "enum": [
            "started",
            "refused"
          ],
          "type": "string"
        },
        {
          "description": "Another trigger's run is active; this one observes it rather than racing.",
          "enum": [
            "observed"
          ],
          "type": "string"
        }
      ]
    }
  },
  "description": "`work/run_briefing` and `work/cancel_briefing` result.",
  "properties": {
    "code": {
      "description": "A stable code for a refusal — `not_configured`, `cooldown`, `provider_unsupported` — never payload or provider text.",
      "type": [
        "string",
        "null"
      ]
    },
    "detail": {
      "type": [
        "string",
        "null"
      ]
    },
    "outcome": {
      "$ref": "#/definitions/WorkBriefReceiptOutcome"
    },
    "runId": {
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "outcome"
  ],
  "title": "WorkBriefReceipt",
  "type": "object"
}
