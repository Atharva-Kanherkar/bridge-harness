{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "The typed answer. `Unknown` is the single shape for both \"this id belongs to nothing\" and \"this id exists but you may not see it\" — resolution never leaks existence across the authorization boundary. `rename_all` on a tagged enum renames the *variants*, never the fields inside struct variants, and schemars 0.8 does not understand serde's `rename_all_fields`. So each multi-word field is renamed explicitly — otherwise this is the one snake_case shape on an all-camelCase wire, and the generated schema and the serialized payload disagree silently.",
  "oneOf": [
    {
      "properties": {
        "activeEntryId": {
          "type": [
            "string",
            "null"
          ]
        },
        "authorized": {
          "type": "boolean"
        },
        "continuationFidelity": {
          "type": "string"
        },
        "depth": {
          "format": "int64",
          "type": "integer"
        },
        "harness": {
          "type": "string"
        },
        "kind": {
          "enum": [
            "session"
          ],
          "type": "string"
        },
        "label": {
          "type": "string"
        },
        "latestCheckpointEntryId": {
          "type": [
            "string",
            "null"
          ]
        },
        "parentSessionId": {
          "type": [
            "string",
            "null"
          ]
        },
        "restorationMode": {
          "type": "string"
        },
        "sessionId": {
          "type": "string"
        },
        "updatedAt": {
          "type": [
            "string",
            "null"
          ]
        },
        "workspaceId": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "authorized",
        "continuationFidelity",
        "depth",
        "harness",
        "kind",
        "label",
        "restorationMode",
        "sessionId"
      ],
      "type": "object"
    },
    {
      "properties": {
        "authorized": {
          "type": "boolean"
        },
        "createdAt": {
          "type": "string"
        },
        "entryId": {
          "type": "string"
        },
        "entryKind": {
          "type": "string"
        },
        "kind": {
          "enum": [
            "entry"
          ],
          "type": "string"
        },
        "sequence": {
          "format": "int64",
          "type": "integer"
        },
        "sessionId": {
          "type": "string"
        },
        "summary": {
          "type": "string"
        }
      },
      "required": [
        "authorized",
        "createdAt",
        "entryId",
        "entryKind",
        "kind",
        "sequence",
        "sessionId",
        "summary"
      ],
      "type": "object"
    },
    {
      "properties": {
        "authorized": {
          "type": "boolean"
        },
        "kind": {
          "enum": [
            "unknown"
          ],
          "type": "string"
        }
      },
      "required": [
        "authorized",
        "kind"
      ],
      "type": "object"
    }
  ],
  "title": "ResolveReferenceResult"
}
