{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "`sessions/get_session_forest_digest`'s result: an opaque change token for one session's forest. Equal digests mean the snapshot would be unchanged; clients compare tokens instead of fetching and stringifying complete histories every poll. External state the store cannot see (repository divergence) is not covered — poll a full snapshot at a low cadence for that.",
  "properties": {
    "digest": {
      "type": "string"
    }
  },
  "required": [
    "digest"
  ],
  "title": "SessionForestDigestResult",
  "type": "object"
}
