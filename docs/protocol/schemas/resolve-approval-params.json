{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "ApprovalDecision": {
      "description": "What the operator decided about a pending approval. The command rejects anything outside this set, so the contract names it: a host validating params from the registry turns a typo into `invalid_params` instead of an application error.",
      "enum": [
        "accept",
        "acceptForSession",
        "decline",
        "cancel"
      ],
      "type": "string"
    }
  },
  "properties": {
    "decision": {
      "$ref": "#/definitions/ApprovalDecision"
    },
    "eventId": {
      "description": "The durable sequence of the `approval.requested` event being answered.",
      "format": "int64",
      "type": "integer"
    },
    "sessionId": {
      "type": "string"
    }
  },
  "required": [
    "decision",
    "eventId",
    "sessionId"
  ],
  "title": "ResolveApprovalParams",
  "type": "object"
}
