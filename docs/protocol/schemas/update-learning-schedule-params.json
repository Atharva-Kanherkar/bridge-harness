{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "LearningSchedule": {
      "description": "The learning job's cadence and per-run budget. Mirrors `bridge_core::learning_job::LearningSchedule`.",
      "properties": {
        "cadenceMinutes": {
          "format": "int64",
          "type": "integer"
        },
        "enabled": {
          "type": "boolean"
        },
        "jobId": {
          "type": "string"
        },
        "mode": {
          "description": "How far a run may go on its own: `manual` recommends, `ask` waits for approval, `automatic` promotes to a guarded canary.",
          "type": "string"
        },
        "nextRunAt": {
          "type": [
            "string",
            "null"
          ]
        },
        "runBudgetMicrousd": {
          "format": "int64",
          "type": "integer"
        },
        "runBudgetTokens": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "cadenceMinutes",
        "enabled",
        "jobId",
        "mode",
        "runBudgetMicrousd",
        "runBudgetTokens"
      ],
      "type": "object"
    }
  },
  "properties": {
    "schedule": {
      "$ref": "#/definitions/LearningSchedule"
    }
  },
  "required": [
    "schedule"
  ],
  "title": "UpdateLearningScheduleParams",
  "type": "object"
}
