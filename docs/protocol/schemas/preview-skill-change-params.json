{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "SkillAction": {
      "description": "What to do to a skill. Mirrors `bridge_core::skill_marketplace::SkillAction`.",
      "enum": [
        "install",
        "rollback",
        "uninstall"
      ],
      "type": "string"
    },
    "SkillProvider": {
      "description": "Whose skill root a skill is installed into. Mirrors `bridge_core::skill_marketplace::SkillProvider`.",
      "enum": [
        "codex",
        "claude",
        "opencode"
      ],
      "type": "string"
    }
  },
  "properties": {
    "action": {
      "$ref": "#/definitions/SkillAction"
    },
    "skillId": {
      "type": "string"
    },
    "targets": {
      "description": "Every provider the change applies to.",
      "items": {
        "$ref": "#/definitions/SkillProvider"
      },
      "type": "array"
    }
  },
  "required": [
    "action",
    "skillId",
    "targets"
  ],
  "title": "PreviewSkillChangeParams",
  "type": "object"
}
