{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "HarnessId": {
      "description": "Which agent runs a session, e.g. 'claude', 'codex', 'gemini'. Identifies the agent only — how Bridge runs it is not encoded here.",
      "pattern": "^[a-z0-9][a-z0-9._-]{0,63}$",
      "type": "string"
    }
  },
  "properties": {
    "effort": {
      "description": "Provider-specific thinking level, validated against the selected model. Omitted preserves the current value when the target model supports it.",
      "type": [
        "string",
        "null"
      ]
    },
    "harness": {
      "$ref": "#/definitions/HarnessId"
    },
    "model": {
      "description": "Explicit model id; omitted selects the harness's default for the chat's tier.",
      "type": [
        "string",
        "null"
      ]
    },
    "sessionId": {
      "type": "string"
    }
  },
  "required": [
    "harness",
    "sessionId"
  ],
  "title": "UpdateChatModelParams",
  "type": "object"
}
