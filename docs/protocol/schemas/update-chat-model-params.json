{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "HarnessId": {
      "description": "A built-in harness ('claude', 'codex', 'opencode', 'shell') or an installed ACP agent ('acp:' + its registry id).",
      "pattern": "^(claude|codex|opencode|shell|acp:[a-z0-9][a-z0-9._-]{0,63})$",
      "type": "string"
    }
  },
  "properties": {
    "harness": {
      "$ref": "#/definitions/HarnessId"
    },
    "model": {
      "description": "Explicit model id; omitted selects the harness's default for the chat's tier.",
      "type": [
        "string",
        "null"
      ]
    },
    "sessionId": {
      "type": "string"
    }
  },
  "required": [
    "harness",
    "sessionId"
  ],
  "title": "UpdateChatModelParams",
  "type": "object"
}
