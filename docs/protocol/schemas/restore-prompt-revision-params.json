{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "PromptTargetChoice": {
      "description": "Which compiled prompt stack a Prompt Studio method operates on. Wire values equal the storage keys the core prompt model uses.",
      "enum": [
        "orchestrator",
        "worker:research",
        "worker:implementation",
        "worker:verification",
        "worker:planning",
        "worker:documentation",
        "direct_session"
      ],
      "type": "string"
    }
  },
  "properties": {
    "depth": {
      "format": "int64",
      "type": [
        "integer",
        "null"
      ]
    },
    "revisionId": {
      "description": "The revision to restore; it must belong to this exact target and section, and restoring appends a new revision rather than rewinding.",
      "format": "int64",
      "type": "integer"
    },
    "sectionId": {
      "type": "string"
    },
    "target": {
      "$ref": "#/definitions/PromptTargetChoice"
    }
  },
  "required": [
    "revisionId",
    "sectionId",
    "target"
  ],
  "title": "RestorePromptRevisionParams",
  "type": "object"
}
