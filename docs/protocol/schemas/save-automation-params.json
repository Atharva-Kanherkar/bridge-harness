{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "AutomationProvider": {
      "description": "Whose native automation store an entry lives in. Mirrors `bridge_core::automations::AutomationProvider`.",
      "enum": [
        "claude",
        "codex",
        "cursor"
      ],
      "type": "string"
    }
  },
  "description": "Create a provider-native automation when `id` is absent, or edit the identified native automation when it is present. Capability checks remain authoritative in bridge-core.",
  "properties": {
    "id": {
      "type": [
        "string",
        "null"
      ]
    },
    "prompt": {
      "type": "string"
    },
    "provider": {
      "$ref": "#/definitions/AutomationProvider"
    },
    "recurring": {
      "type": "boolean"
    },
    "scheduleExpression": {
      "type": "string"
    }
  },
  "required": [
    "prompt",
    "provider",
    "recurring",
    "scheduleExpression"
  ],
  "title": "SaveAutomationParams",
  "type": "object"
}
