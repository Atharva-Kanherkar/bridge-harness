{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "RequestId": {
      "anyOf": [
        {
          "format": "int64",
          "type": "integer"
        },
        {
          "type": "string"
        }
      ],
      "description": "A request identifier. Clients choose; servers echo it back verbatim."
    },
    "RpcError": {
      "properties": {
        "code": {
          "description": "A stable code from the [`crate::error::ErrorCode`] registry.",
          "format": "int64",
          "type": "integer"
        },
        "data": true,
        "message": {
          "type": "string"
        }
      },
      "required": [
        "code",
        "message"
      ],
      "type": "object"
    }
  },
  "description": "Exactly one of `result` / `error` is present; constructors uphold the invariant and [`RpcResponse::is_well_formed`] checks a decoded value.",
  "properties": {
    "error": {
      "anyOf": [
        {
          "$ref": "#/definitions/RpcError"
        },
        {
          "type": "null"
        }
      ]
    },
    "id": {
      "$ref": "#/definitions/RequestId"
    },
    "jsonrpc": {
      "description": "Always `\"2.0\"`.",
      "type": "string"
    },
    "result": true
  },
  "required": [
    "id",
    "jsonrpc"
  ],
  "title": "RpcResponse",
  "type": "object"
}
