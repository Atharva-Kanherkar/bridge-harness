{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "HarnessId": {
      "description": "A harness identifier on the wire. Mirrors `bridge_core::model::Harness` variant for variant; an exhaustive conversion in bridge-core keeps the two from drifting.",
      "enum": [
        "claude",
        "codex",
        "opencode",
        "shell"
      ],
      "type": "string"
    }
  },
  "properties": {
    "harness": {
      "anyOf": [
        {
          "$ref": "#/definitions/HarnessId"
        },
        {
          "type": "null"
        }
      ],
      "description": "Explicit harness; omitted resolves the configured orchestrator."
    },
    "model": {
      "type": [
        "string",
        "null"
      ]
    },
    "workspaceId": {
      "type": "string"
    }
  },
  "required": [
    "workspaceId"
  ],
  "title": "StartSessionParams",
  "type": "object"
}
