{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "MemoryLedgerCapability": {
      "description": "The Bridge half: the local ledger's contract.",
      "properties": {
        "exists": {
          "type": "boolean"
        },
        "kinds": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "maxBodyChars": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "scopeKey": {
          "type": "string"
        }
      },
      "required": [
        "exists",
        "kinds",
        "maxBodyChars",
        "scopeKey"
      ],
      "type": "object"
    },
    "ProviderMemoryCommand": {
      "description": "A provider-owned memory command in the catalog. It stays on that provider.",
      "properties": {
        "command": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "harness": {
          "type": "string"
        }
      },
      "required": [
        "command",
        "description",
        "harness"
      ],
      "type": "object"
    }
  },
  "description": "What memory exists, so surfaces can be honest about what they do not own.",
  "properties": {
    "ledger": {
      "$ref": "#/definitions/MemoryLedgerCapability"
    },
    "providerNative": {
      "description": "Provider-owned memory commands currently reachable through the slash catalog. An unavailable adapter contributes nothing.",
      "items": {
        "$ref": "#/definitions/ProviderMemoryCommand"
      },
      "type": "array"
    }
  },
  "required": [
    "ledger",
    "providerNative"
  ],
  "title": "MemoryCapabilities",
  "type": "object"
}
