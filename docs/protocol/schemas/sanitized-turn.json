{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "SecretInterception": {
      "description": "Mirrors `bridge_core::secret_interception::SecretInterception` — one secret replaced by a broker reference before the turn left the machine.",
      "properties": {
        "detector": {
          "type": "string"
        },
        "reference": {
          "description": "The broker reference substituted into the text; the secret itself never crosses the wire.",
          "type": "string"
        }
      },
      "required": [
        "detector",
        "reference"
      ],
      "type": "object"
    }
  },
  "description": "`sessions/prepare_turn`'s result. Mirrors `bridge_core::secret_interception::SanitizedTurn`.",
  "properties": {
    "interceptions": {
      "items": {
        "$ref": "#/definitions/SecretInterception"
      },
      "type": "array"
    },
    "text": {
      "type": "string"
    }
  },
  "required": [
    "interceptions",
    "text"
  ],
  "title": "SanitizedTurn",
  "type": "object"
}
