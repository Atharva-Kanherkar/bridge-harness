{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "Effort": {
      "description": "A reasoning-effort level on the wire. Mirrors `bridge_core::delegation::Effort` variant for variant; a mirror test in bridge-core keeps the two from drifting.",
      "enum": [
        "low",
        "medium",
        "high",
        "xhigh"
      ],
      "type": "string"
    },
    "ModelProfileDraft": {
      "additionalProperties": false,
      "description": "A profile as the operator edits it, before the server stamps a version. Mirrors `bridge_core::model_profiles::ModelProfileDraft`, including its refusal of unknown fields.",
      "properties": {
        "budgetPreference": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "effort": {
          "$ref": "#/definitions/Effort"
        },
        "fallbackPurpose": {
          "anyOf": [
            {
              "$ref": "#/definitions/ProfilePurpose"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Purpose to fall back to when this one cannot run."
        },
        "latencyPreference": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "learningEnabled": {
          "type": "boolean"
        },
        "model": {
          "type": "string"
        },
        "pinned": {
          "description": "Pinned profiles are exempt from learned routing.",
          "type": "boolean"
        },
        "provider": {
          "type": "string"
        },
        "purpose": {
          "$ref": "#/definitions/ProfilePurpose"
        }
      },
      "required": [
        "effort",
        "learningEnabled",
        "model",
        "pinned",
        "provider",
        "purpose"
      ],
      "type": "object"
    },
    "ProfilePurpose": {
      "description": "The role a profile fills. Mirrors `bridge_core::model_profiles::ProfilePurpose`.",
      "enum": [
        "standard_orchestrator",
        "premium_orchestrator",
        "planner",
        "implementer",
        "verifier",
        "reviewer",
        "research",
        "documentation",
        "evaluator"
      ],
      "type": "string"
    }
  },
  "properties": {
    "profiles": {
      "description": "The complete profile set; saving replaces what is stored.",
      "items": {
        "$ref": "#/definitions/ModelProfileDraft"
      },
      "type": "array"
    }
  },
  "required": [
    "profiles"
  ],
  "title": "SaveModelProfilesParams",
  "type": "object"
}
