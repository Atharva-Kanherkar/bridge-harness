{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "ClientInfo": {
      "properties": {
        "name": {
          "type": "string"
        },
        "version": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "version"
      ],
      "type": "object"
    },
    "ProtocolVersion": {
      "properties": {
        "major": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "minor": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "major",
        "minor"
      ],
      "type": "object"
    }
  },
  "properties": {
    "authToken": {
      "description": "The per-install authentication token, required by hosts that serve remote-capable transports (the `bridged` daemon). Clients read it from the token file in the data directory; it never appears in any response. Version negotiation itself ignores it — enforcement is the host's.",
      "type": [
        "string",
        "null"
      ]
    },
    "client": {
      "$ref": "#/definitions/ClientInfo"
    },
    "protocolVersion": {
      "$ref": "#/definitions/ProtocolVersion"
    }
  },
  "required": [
    "client",
    "protocolVersion"
  ],
  "title": "HandshakeRequest",
  "type": "object"
}
