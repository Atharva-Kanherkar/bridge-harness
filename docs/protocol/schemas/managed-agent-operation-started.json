{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "ManagedAgentOperationKind": {
      "description": "Which lifecycle operation a progress stream belongs to.",
      "enum": [
        "install",
        "repair",
        "uninstall"
      ],
      "type": "string"
    }
  },
  "description": "An accepted operation.\n\nThe id correlates progress notifications. It is not a completion: a client that misses the terminal notification refetches authoritative state rather than inferring it from progress it happened to see.",
  "properties": {
    "agentId": {
      "type": "string"
    },
    "kind": {
      "$ref": "#/definitions/ManagedAgentOperationKind"
    },
    "operationId": {
      "type": "string"
    }
  },
  "required": [
    "agentId",
    "kind",
    "operationId"
  ],
  "title": "ManagedAgentOperationStarted",
  "type": "object"
}
