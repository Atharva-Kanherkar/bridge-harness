{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "A session prepared from a task, with nothing sent.\n\nThere is no turn id here, and that absence is the contract: preparing creates a draft the user edits and sends. A field naming a dispatched turn would mean this call had already spoken to a model on their behalf.",
  "properties": {
    "draft": {
      "description": "The composer's starting contents, carrying untrusted task text as text.",
      "type": "string"
    },
    "sessionId": {
      "type": "string"
    },
    "title": {
      "type": "string"
    }
  },
  "required": [
    "draft",
    "sessionId",
    "title"
  ],
  "title": "WorkTaskDraft",
  "type": "object"
}
