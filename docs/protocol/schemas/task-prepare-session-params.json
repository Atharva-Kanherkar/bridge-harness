{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "HarnessId": {
      "description": "Which agent runs a session, e.g. 'claude', 'codex', 'gemini'. Identifies the agent only — how Bridge runs it is not encoded here.",
      "pattern": "^[a-z0-9][a-z0-9._-]{0,63}$",
      "type": "string"
    }
  },
  "properties": {
    "harness": {
      "allOf": [
        {
          "$ref": "#/definitions/HarnessId"
        }
      ],
      "description": "Which harness the prepared session will use when the user eventually sends.\n\nChosen by the caller, the same way a new chat's harness is: availability is a frontend concern, and inventing a different default here would give a task-started session a provider the user never picks anywhere else."
    },
    "model": {
      "type": [
        "string",
        "null"
      ]
    },
    "taskId": {
      "type": "string"
    }
  },
  "required": [
    "harness",
    "taskId"
  ],
  "title": "TaskPrepareSessionParams",
  "type": "object"
}
