{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "HarnessId": {
      "description": "Which agent runs a session, e.g. 'claude', 'codex', 'gemini'. Identifies the agent only — how Bridge runs it is not encoded here.",
      "pattern": "^[a-z0-9][a-z0-9._-]{0,63}$",
      "type": "string"
    }
  },
  "description": "`sessions/fork_session`'s request: branch a session's conversation at an entry into a new, independent session that begins with the parent's history up to that point. The parent is never modified.",
  "properties": {
    "entryId": {
      "type": "string"
    },
    "harness": {
      "anyOf": [
        {
          "$ref": "#/definitions/HarnessId"
        },
        {
          "type": "null"
        }
      ],
      "description": "Inherits the parent's harness when omitted."
    },
    "model": {
      "description": "Inherits the parent's model when omitted.",
      "type": [
        "string",
        "null"
      ]
    },
    "sessionId": {
      "type": "string"
    },
    "title": {
      "type": [
        "string",
        "null"
      ]
    },
    "worktreePolicy": {
      "default": "shared",
      "description": "`\"shared\"` — both sessions work the same repo path (default). `\"new\"` — the fork gets its own Git worktree and branch.",
      "type": "string"
    }
  },
  "required": [
    "entryId",
    "sessionId"
  ],
  "title": "ForkSessionParams",
  "type": "object"
}
