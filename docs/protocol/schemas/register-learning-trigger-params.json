{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "LearningTriggerKind": {
      "description": "What woke a learning run. Mirrors `bridge_core::learning_job::LearningTriggerKind` — including its wire spelling, so `OpenCode` is `open_code` here as it is there.",
      "enum": [
        "manual",
        "in_app",
        "codex",
        "claude",
        "open_code"
      ],
      "type": "string"
    }
  },
  "properties": {
    "credentialRef": {
      "description": "Reference to a stored credential; the credential itself never crosses the wire.",
      "type": [
        "string",
        "null"
      ]
    },
    "expiresAt": {
      "type": [
        "string",
        "null"
      ]
    },
    "kind": {
      "allOf": [
        {
          "$ref": "#/definitions/LearningTriggerKind"
        }
      ],
      "description": "Only the external harness triggers may be registered."
    },
    "registrationId": {
      "type": "string"
    }
  },
  "required": [
    "kind",
    "registrationId"
  ],
  "title": "RegisterLearningTriggerParams",
  "type": "object"
}
