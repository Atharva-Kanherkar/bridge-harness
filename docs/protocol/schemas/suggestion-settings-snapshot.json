{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "SuggestionSettings": {
      "additionalProperties": false,
      "description": "The composer typeahead's configuration. A small standalone blob rather than a `ProfilePurpose` — that enum is closed and mirrored across three layers with role semantics (\"what runs the orchestrator\") that a keystroke-driven draft completion does not share.",
      "properties": {
        "enabled": {
          "description": "Off by default: nothing about the user's draft reaches a model until they opt in.",
          "type": "boolean"
        },
        "model": {
          "type": "string"
        },
        "provider": {
          "type": "string"
        }
      },
      "required": [
        "enabled",
        "model",
        "provider"
      ],
      "type": "object"
    }
  },
  "description": "`models/get_suggestion_settings`' and `models/save_suggestion_settings`' result. `configured` mirrors the Work settings pattern: `false` is a fresh install reading defaults, `true` is a user who has saved this at least once (including saving it switched off).",
  "properties": {
    "configured": {
      "type": "boolean"
    },
    "settings": {
      "$ref": "#/definitions/SuggestionSettings"
    }
  },
  "required": [
    "configured",
    "settings"
  ],
  "title": "SuggestionSettingsSnapshot",
  "type": "object"
}
