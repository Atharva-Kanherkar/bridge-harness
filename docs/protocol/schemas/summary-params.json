{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "UsageResolution": {
      "description": "Mirrors `bridge_core::usage_summary::UsageResolution`.",
      "enum": [
        "day",
        "hour"
      ],
      "type": "string"
    }
  },
  "properties": {
    "includeDashboard": {
      "default": false,
      "description": "Opt in to account dashboard history and nullable session counts. Older clients omit this and retain the original local-only result.",
      "type": "boolean"
    },
    "includeImported": {
      "description": "Merge observations imported from provider transcripts, de-duplicated against live rows by provider session.",
      "type": "boolean"
    },
    "resolution": {
      "$ref": "#/definitions/UsageResolution"
    },
    "sinceDay": {
      "description": "Inclusive first day, `YYYY-MM-DD` in `timeZone`.",
      "type": "string"
    },
    "sinceTime": {
      "description": "Inclusive UTC start, RFC 3339. Required for `hour` resolution.",
      "type": [
        "string",
        "null"
      ]
    },
    "timeZone": {
      "description": "IANA zone to bucket days in; UTC when absent.",
      "type": [
        "string",
        "null"
      ]
    },
    "untilDay": {
      "description": "Inclusive last day, `YYYY-MM-DD` in `timeZone`.",
      "type": "string"
    },
    "untilTime": {
      "description": "Exclusive UTC end, RFC 3339, at most 24 hours after `sinceTime`. Required for `hour` resolution.",
      "type": [
        "string",
        "null"
      ]
    },
    "workspaceId": {
      "description": "Restrict live rows to one workspace. Imported observations are device-wide and are never scoped.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "includeImported",
    "resolution",
    "sinceDay",
    "untilDay"
  ],
  "title": "SummaryParams",
  "type": "object"
}
