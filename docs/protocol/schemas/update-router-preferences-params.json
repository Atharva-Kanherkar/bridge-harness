{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "RouterMode": {
      "description": "How much authority the router has. Mirrors `bridge_core::learning_router::RouterMode`.",
      "enum": [
        "disabled",
        "shadow",
        "autonomous"
      ],
      "type": "string"
    },
    "RouterPreferences": {
      "additionalProperties": false,
      "description": "Per-workspace routing preferences. Mirrors `bridge_core::learning_router::RouterPreferences`, including its refusal of unknown fields — a misspelled preference must not silently do nothing.",
      "properties": {
        "excludedHarnesses": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "excludedModels": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "minimumPassBps": {
          "description": "Minimum expected pass rate in basis points; the server rejects values above 10000 (100%).",
          "format": "uint16",
          "maximum": 10000.0,
          "minimum": 0.0,
          "type": "integer"
        },
        "mode": {
          "$ref": "#/definitions/RouterMode"
        },
        "pinnedHarness": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "pinnedModel": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "minimumPassBps",
        "mode"
      ],
      "type": "object"
    }
  },
  "properties": {
    "preferences": {
      "$ref": "#/definitions/RouterPreferences"
    },
    "workspaceId": {
      "type": "string"
    }
  },
  "required": [
    "preferences",
    "workspaceId"
  ],
  "title": "UpdateRouterPreferencesParams",
  "type": "object"
}
