{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Params for `usage/insights`: the window to analyse and whether to run the model again. Without `refresh` the last stored report for any window is returned when one exists, so opening the tab costs nothing.",
  "properties": {
    "refresh": {
      "default": false,
      "description": "Run the analysis again even when a stored report exists.",
      "type": "boolean"
    },
    "windowDays": {
      "description": "Days of history to read, ending today. Clamped to 1..=90 by the core.",
      "format": "int64",
      "type": "integer"
    }
  },
  "required": [
    "windowDays"
  ],
  "title": "InsightsParams",
  "type": "object"
}
