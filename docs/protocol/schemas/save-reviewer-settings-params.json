{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "Effort": {
      "description": "A reasoning-effort level on the wire. Mirrors `bridge_core::delegation::Effort` variant for variant; a mirror test in bridge-core keeps the two from drifting.",
      "enum": [
        "low",
        "medium",
        "high",
        "xhigh"
      ],
      "type": "string"
    },
    "ReviewerHarnessSettings": {
      "additionalProperties": false,
      "description": "What the GitHub pull-request reviewer runs as on one harness. `None` falls through to the Reviewer model profile, then the harness default.",
      "properties": {
        "effort": {
          "anyOf": [
            {
              "$ref": "#/definitions/Effort"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "model": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ReviewerSettings": {
      "additionalProperties": false,
      "description": "Global settings for the subagent that reviews pull requests from the GitHub pane: per-harness model and effort, and the instructions it is given. One record, not per workspace — a reviewer's shape is a preference about the reviewer, not about the repository.",
      "properties": {
        "harnesses": {
          "additionalProperties": {
            "$ref": "#/definitions/ReviewerHarnessSettings"
          },
          "default": {},
          "description": "Keyed by harness id (`claude` | `codex` | `opencode`).",
          "type": "object"
        },
        "systemPrompt": {
          "default": "",
          "description": "Empty means Bridge's default review instructions. `{number}` expands to the pull request number.",
          "type": "string"
        }
      },
      "type": "object"
    }
  },
  "properties": {
    "settings": {
      "$ref": "#/definitions/ReviewerSettings"
    }
  },
  "required": [
    "settings"
  ],
  "title": "SaveReviewerSettingsParams",
  "type": "object"
}
