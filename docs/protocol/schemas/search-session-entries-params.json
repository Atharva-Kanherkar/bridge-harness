{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "FTS5 recall over `session_entries`. `sessionId` is required; there is no workspace-wide search on this method.",
  "properties": {
    "limit": {
      "description": "Omitted requests use 20; the server rejects values outside 1..=50.",
      "format": "uint32",
      "maximum": 50.0,
      "minimum": 1.0,
      "type": [
        "integer",
        "null"
      ]
    },
    "query": {
      "type": "string"
    },
    "sessionId": {
      "type": "string"
    }
  },
  "required": [
    "query",
    "sessionId"
  ],
  "title": "SearchSessionEntriesParams",
  "type": "object"
}
