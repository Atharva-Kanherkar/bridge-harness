{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "GithubAction": {
      "description": "One mutating GitHub action. The `kind` tag selects the variant; every write path is expressed here so a client cannot smuggle a free-form `gh` argument.",
      "oneOf": [
        {
          "additionalProperties": false,
          "properties": {
            "kind": {
              "enum": [
                "merge"
              ],
              "type": "string"
            },
            "number": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "strategy": {
              "$ref": "#/definitions/MergeStrategy"
            }
          },
          "required": [
            "kind",
            "number",
            "strategy"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "body": {
              "type": "string"
            },
            "event": {
              "$ref": "#/definitions/ReviewEvent"
            },
            "kind": {
              "enum": [
                "review"
              ],
              "type": "string"
            },
            "number": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "body",
            "event",
            "kind",
            "number"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "body": {
              "type": "string"
            },
            "commentId": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            },
            "kind": {
              "enum": [
                "reply"
              ],
              "type": "string"
            },
            "number": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "body",
            "commentId",
            "kind",
            "number"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "kind": {
              "enum": [
                "rerun"
              ],
              "type": "string"
            },
            "number": {
              "format": "uint64",
              "minimum": 0.0,
              "type": "integer"
            }
          },
          "required": [
            "kind",
            "number"
          ],
          "type": "object"
        }
      ]
    },
    "MergeStrategy": {
      "enum": [
        "merge",
        "squash",
        "rebase"
      ],
      "type": "string"
    },
    "ReviewEvent": {
      "enum": [
        "approve",
        "requestChanges",
        "comment"
      ],
      "type": "string"
    }
  },
  "properties": {
    "action": {
      "$ref": "#/definitions/GithubAction"
    },
    "confirmed": {
      "description": "The outcome of the native per-action confirmation. `false` means the user declined; the backend then executes nothing.",
      "type": "boolean"
    },
    "workspaceId": {
      "type": "string"
    }
  },
  "required": [
    "action",
    "confirmed",
    "workspaceId"
  ],
  "title": "GithubActParams",
  "type": "object"
}
