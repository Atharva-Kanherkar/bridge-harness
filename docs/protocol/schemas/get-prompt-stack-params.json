{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "PromptTargetChoice": {
      "description": "Which compiled prompt stack a Prompt Studio method operates on. Wire values equal the storage keys the core prompt model uses.",
      "enum": [
        "orchestrator",
        "worker:research",
        "worker:implementation",
        "worker:verification",
        "worker:planning",
        "worker:documentation",
        "direct_session"
      ],
      "type": "string"
    }
  },
  "description": "Every Prompt Studio method addresses one target; `depth` selects the worker topology depth for depth-sensitive worker contracts, is ignored by non-worker targets, and defaults to 0 when omitted. Each method repeats these fields because params types are named after their method.",
  "properties": {
    "depth": {
      "format": "int64",
      "type": [
        "integer",
        "null"
      ]
    },
    "target": {
      "$ref": "#/definitions/PromptTargetChoice"
    }
  },
  "required": [
    "target"
  ],
  "title": "GetPromptStackParams",
  "type": "object"
}
