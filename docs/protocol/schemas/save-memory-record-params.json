{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Explicit pin. Scope is always written as `account:local` by the server.",
  "properties": {
    "body": {
      "type": "string"
    },
    "kind": {
      "description": "`preference` (default), `fact`, `decision`, or `constraint`.",
      "type": [
        "string",
        "null"
      ]
    },
    "sessionId": {
      "description": "Optional session that originated the slash/command. Not a scope key.",
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "body"
  ],
  "title": "SaveMemoryRecordParams",
  "type": "object"
}
