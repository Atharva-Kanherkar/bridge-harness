{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "GithubRepository": {
      "properties": {
        "host": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "owner": {
          "type": "string"
        }
      },
      "required": [
        "host",
        "name",
        "owner"
      ],
      "type": "object"
    }
  },
  "properties": {
    "initialized": {
      "description": "`true` when the folder had no git repository and one was initialized.",
      "type": "boolean"
    },
    "replacedRemote": {
      "description": "`true` when an existing `origin` was repointed rather than added.",
      "type": "boolean"
    },
    "repository": {
      "allOf": [
        {
          "$ref": "#/definitions/GithubRepository"
        }
      ],
      "description": "The repository the workspace now resolves to — proof the connection took, not just that the git commands exited zero."
    }
  },
  "required": [
    "initialized",
    "replacedRemote",
    "repository"
  ],
  "title": "GithubConnectResult",
  "type": "object"
}
