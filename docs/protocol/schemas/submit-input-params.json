{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "TurnImage": {
      "additionalProperties": false,
      "description": "One image a user attached to a submitted turn.\n\nImages travel as base64 in-band rather than by path: the clipboard source may never have touched the filesystem, and the wire stays provider-neutral — each adapter decides what the pair becomes (Anthropic image content blocks for Claude). Bytes are transport payload, so secret interception and `@file` context never see them.",
      "properties": {
        "base64Data": {
          "description": "Raw base64 of the encoded image, without the data-URI prefix.",
          "type": "string"
        },
        "mediaType": {
          "description": "RFC 2046 media type, e.g. `image/png`.",
          "type": "string"
        }
      },
      "required": [
        "base64Data",
        "mediaType"
      ],
      "type": "object"
    }
  },
  "properties": {
    "attachments": {
      "description": "Image attachments pasted or otherwise added in the composer. Absent (or `None`) means none: older clients omit it and stay on plain text.",
      "items": {
        "$ref": "#/definitions/TurnImage"
      },
      "type": [
        "array",
        "null"
      ]
    },
    "sessionId": {
      "type": "string"
    },
    "text": {
      "type": "string"
    }
  },
  "required": [
    "sessionId",
    "text"
  ],
  "title": "SubmitInputParams",
  "type": "object"
}
