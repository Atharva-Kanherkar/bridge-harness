{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Abandon a provider's login flow. The vendor process is stopped so the next `start_provider_login` spawns a fresh one and replays its browser handoff, rather than reattaching to a runtime whose output the pane has already lost.",
  "properties": {
    "provider": {
      "type": "string"
    }
  },
  "required": [
    "provider"
  ],
  "title": "CancelProviderLoginParams",
  "type": "object"
}
