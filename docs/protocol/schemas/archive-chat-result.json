{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "What archiving a chat did. Mirrors `bridge_core::worktree_registry::ArchiveChatResult`.\n\n`worktreeDetail` is set when the chat's checkout was *kept* — archiving a conversation does not require first resolving its uncommitted work, so the reason comes back rather than the archive failing.",
  "properties": {
    "archived": {
      "type": "boolean"
    },
    "bytesFreed": {
      "format": "int64",
      "type": "integer"
    },
    "worktreeDetail": {
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "archived",
    "bytesFreed"
  ],
  "title": "ArchiveChatResult",
  "type": "object"
}
