{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "ConnectorActionRequest": {
      "description": "The write the user is asking for.",
      "oneOf": [
        {
          "properties": {
            "kind": {
              "enum": [
                "reply"
              ],
              "type": "string"
            },
            "text": {
              "type": "string"
            }
          },
          "required": [
            "kind",
            "text"
          ],
          "type": "object"
        },
        {
          "properties": {
            "emoji": {
              "type": "string"
            },
            "kind": {
              "enum": [
                "react"
              ],
              "type": "string"
            }
          },
          "required": [
            "emoji",
            "kind"
          ],
          "type": "object"
        }
      ]
    }
  },
  "properties": {
    "action": {
      "$ref": "#/definitions/ConnectorActionRequest"
    },
    "approved": {
      "default": null,
      "description": "The user's decision on the exact effect this call already described.\n\nAbsent means \"I have not decided\": the host refuses and returns the effect for a confirmation dialog. Approval is per call and never sticky — there is no \"always allow\" here, because the thing being approved is a specific string going to a specific place.",
      "type": [
        "boolean",
        "null"
      ]
    },
    "itemKey": {
      "type": "string"
    }
  },
  "required": [
    "action",
    "itemKey"
  ],
  "title": "ConnectorActParams",
  "type": "object"
}
