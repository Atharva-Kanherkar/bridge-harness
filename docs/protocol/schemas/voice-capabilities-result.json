{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "VoiceAudioEncoding": {
      "description": "Encoding Bridge currently accepts from the webview capture pipeline.",
      "enum": [
        "pcm_s16_le"
      ],
      "type": "string"
    },
    "VoiceAvailability": {
      "enum": [
        "ready",
        "needsSetup",
        "unsupported",
        "failed"
      ],
      "type": "string"
    },
    "VoiceProcessingLocation": {
      "enum": [
        "onDevice",
        "remote"
      ],
      "type": "string"
    },
    "VoiceProviderCapability": {
      "properties": {
        "channels": {
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        },
        "encoding": {
          "$ref": "#/definitions/VoiceAudioEncoding"
        },
        "maxChunkBytes": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "maxSessionBytes": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "processing": {
          "$ref": "#/definitions/VoiceProcessingLocation"
        },
        "provider": {
          "$ref": "#/definitions/VoiceProviderId"
        },
        "recoveryAction": {
          "anyOf": [
            {
              "$ref": "#/definitions/VoiceRecoveryAction"
            },
            {
              "type": "null"
            }
          ]
        },
        "sampleRate": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "state": {
          "$ref": "#/definitions/VoiceAvailability"
        },
        "supportedLocales": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "unavailableReason": {
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "channels",
        "encoding",
        "maxChunkBytes",
        "maxSessionBytes",
        "processing",
        "provider",
        "sampleRate",
        "state",
        "supportedLocales"
      ],
      "type": "object"
    },
    "VoiceProviderId": {
      "enum": [
        "local",
        "codex"
      ],
      "type": "string"
    },
    "VoiceRecoveryAction": {
      "enum": [
        "setup",
        "retry"
      ],
      "type": "string"
    }
  },
  "properties": {
    "providers": {
      "items": {
        "$ref": "#/definitions/VoiceProviderCapability"
      },
      "type": "array"
    },
    "selectedProvider": {
      "anyOf": [
        {
          "$ref": "#/definitions/VoiceProviderId"
        },
        {
          "type": "null"
        }
      ]
    },
    "sessionId": {
      "type": [
        "string",
        "null"
      ]
    }
  },
  "required": [
    "providers"
  ],
  "title": "VoiceCapabilitiesResult",
  "type": "object"
}
