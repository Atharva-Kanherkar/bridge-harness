{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "What an explicit reclaim did, or why it did not. Mirrors `bridge_core::worktree_registry::WorktreeReclaimResult`.\n\nA refusal is a *result*, not an error: \"no, and here is why\" is the useful answer for a button, and `detail` carries the reason in words.",
  "properties": {
    "bytesFreed": {
      "format": "int64",
      "type": "integer"
    },
    "detail": {
      "type": [
        "string",
        "null"
      ]
    },
    "disposition": {
      "type": "string"
    },
    "reclaimed": {
      "type": "boolean"
    }
  },
  "required": [
    "bytesFreed",
    "disposition",
    "reclaimed"
  ],
  "title": "WorktreeReclaimResult",
  "type": "object"
}
