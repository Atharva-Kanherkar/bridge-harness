{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "ProtocolVersion": {
      "properties": {
        "major": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        },
        "minor": {
          "format": "uint32",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "major",
        "minor"
      ],
      "type": "object"
    },
    "ServerInfo": {
      "properties": {
        "name": {
          "type": "string"
        },
        "version": {
          "type": "string"
        }
      },
      "required": [
        "name",
        "version"
      ],
      "type": "object"
    }
  },
  "properties": {
    "buildId": {
      "description": "A content-addressed identity of the server's own executable, so a launcher holding a newer binary can tell it is talking to a stale daemon. `ServerInfo.version` cannot: it is the workspace version, a constant on every dev build. Optional — a host that has none (tests, the embedded host, older daemons) omits it, and a launcher treats the absence as stale when it has a binary of its own to offer.",
      "type": [
        "string",
        "null"
      ]
    },
    "capabilities": {
      "description": "The method domains this server serves (sorted, deduplicated).",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "protocolVersion": {
      "$ref": "#/definitions/ProtocolVersion"
    },
    "server": {
      "$ref": "#/definitions/ServerInfo"
    }
  },
  "required": [
    "capabilities",
    "protocolVersion",
    "server"
  ],
  "title": "HandshakeResponse",
  "type": "object"
}
