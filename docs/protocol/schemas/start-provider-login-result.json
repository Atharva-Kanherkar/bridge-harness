{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Identifies the terminal pane hosting the vendor's login flow — the same `workspaceId`/`terminalId` pair the existing terminal domain methods (`terminal/write_terminal`, `terminal/resize_terminal`, `terminal/close_terminal`) address, so the UI hosts the flow without a second PTY surface.",
  "properties": {
    "terminalId": {
      "type": "string"
    },
    "workspaceId": {
      "type": "string"
    }
  },
  "required": [
    "terminalId",
    "workspaceId"
  ],
  "title": "StartProviderLoginResult",
  "type": "object"
}
