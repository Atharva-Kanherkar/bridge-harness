{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "SuggestionFallbackReason": {
      "description": "Why the configured suggestion model was skipped in favour of the fallback.",
      "enum": [
        "unknown_model",
        "unauthorized",
        "rate_limited"
      ],
      "type": "string"
    }
  },
  "properties": {
    "fallbackReason": {
      "anyOf": [
        {
          "$ref": "#/definitions/SuggestionFallbackReason"
        },
        {
          "type": "null"
        }
      ]
    },
    "suggestion": {
      "description": "The suggested continuation of `text`, or empty when the model had nothing to add. Never includes the draft itself.",
      "type": "string"
    },
    "usedFallback": {
      "type": "boolean"
    }
  },
  "required": [
    "suggestion",
    "usedFallback"
  ],
  "title": "SuggestCompletionResult",
  "type": "object"
}
