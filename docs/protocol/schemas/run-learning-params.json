{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "LearningTriggerKind": {
      "description": "What woke a learning run. Mirrors `bridge_core::learning_job::LearningTriggerKind` — including its wire spelling, so `OpenCode` is `open_code` here as it is there.",
      "enum": [
        "manual",
        "in_app",
        "codex",
        "claude",
        "open_code"
      ],
      "type": "string"
    }
  },
  "properties": {
    "triggerKind": {
      "$ref": "#/definitions/LearningTriggerKind"
    }
  },
  "required": [
    "triggerKind"
  ],
  "title": "RunLearningParams",
  "type": "object"
}
