{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "MemoryRecord": {
      "properties": {
        "body": {
          "type": "string"
        },
        "confidenceBps": {
          "description": "Written only by producers that can mean it (the extractor). An explicit save keeps it NULL, and surfaces render unknown — never a fake number.",
          "format": "uint32",
          "minimum": 0.0,
          "type": [
            "integer",
            "null"
          ]
        },
        "conflictGroup": {
          "description": "Records making competing claims about one subject share this key. At most one member of a group is active, so a packet can never carry two.",
          "type": [
            "string",
            "null"
          ]
        },
        "createdAt": {
          "type": "string"
        },
        "expiresAt": {
          "description": "When set, the instant after which the record stops applying. The sweep closes the interval at this instant rather than at the moment it ran.",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "type": "string"
        },
        "kind": {
          "type": "string"
        },
        "provenance": {
          "type": "string"
        },
        "rationale": {
          "type": [
            "string",
            "null"
          ]
        },
        "scopeKey": {
          "type": "string"
        },
        "sourceSessionId": {
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "type": "string"
        },
        "supersedes": {
          "description": "Set when this record replaced an earlier one through supersede.",
          "type": [
            "string",
            "null"
          ]
        },
        "updatedAt": {
          "type": "string"
        },
        "validFrom": {
          "description": "The instant this record's claim began to hold. Validity is an interval, not a flag: a record that never reached active carries an empty one, where `validTo` equals `validFrom`.",
          "type": "string"
        },
        "validTo": {
          "description": "The instant the claim stopped holding. Absent while the record is active; a supersession, a tombstone, or an expiry closes it, and a closed record stays queryable as history.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "body",
        "createdAt",
        "id",
        "kind",
        "provenance",
        "scopeKey",
        "status",
        "updatedAt",
        "validFrom"
      ],
      "type": "object"
    }
  },
  "properties": {
    "records": {
      "items": {
        "$ref": "#/definitions/MemoryRecord"
      },
      "type": "array"
    },
    "scopeKey": {
      "type": "string"
    }
  },
  "required": [
    "records",
    "scopeKey"
  ],
  "title": "ListMemoryRecordsResult",
  "type": "object"
}
