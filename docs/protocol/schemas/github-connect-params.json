{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Point a workspace at a GitHub repository. The pane offers this instead of reporting a resolution failure: a folder that is not a git repository yet, or one with no GitHub remote, is a setup step rather than an error.",
  "properties": {
    "remoteUrl": {
      "description": "A GitHub HTTPS or SSH remote URL. Validated before any git call runs.",
      "type": "string"
    },
    "workspaceId": {
      "type": "string"
    }
  },
  "required": [
    "remoteUrl",
    "workspaceId"
  ],
  "title": "GithubConnectParams",
  "type": "object"
}
