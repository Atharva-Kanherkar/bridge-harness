{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "definitions": {
    "AutomationAction": {
      "description": "What to do to an automation. Mirrors `bridge_core::automations::AutomationAction`. Pause/resume are only honored where the native format has a paused state (Codex).",
      "enum": [
        "pause",
        "resume",
        "delete"
      ],
      "type": "string"
    },
    "AutomationProvider": {
      "description": "Whose native automation store an entry lives in. Mirrors `bridge_core::automations::AutomationProvider`.",
      "enum": [
        "claude",
        "codex",
        "opencode"
      ],
      "type": "string"
    }
  },
  "properties": {
    "action": {
      "$ref": "#/definitions/AutomationAction"
    },
    "id": {
      "description": "The automation's id in its native store.",
      "type": "string"
    },
    "provider": {
      "$ref": "#/definitions/AutomationProvider"
    }
  },
  "required": [
    "action",
    "id",
    "provider"
  ],
  "title": "ExecuteAutomationActionParams",
  "type": "object"
}
