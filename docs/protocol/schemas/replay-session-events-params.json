{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "properties": {
    "afterSequence": {
      "description": "The last durable sequence the client has seen; events strictly after this cursor are returned in order, with no gaps and no duplicates.",
      "format": "int64",
      "minimum": 0.0,
      "type": "integer"
    },
    "limit": {
      "description": "Maximum number of events to return. Omitted requests use 500; the server rejects values outside 1..=1000.",
      "format": "uint32",
      "maximum": 1000.0,
      "minimum": 1.0,
      "type": [
        "integer",
        "null"
      ]
    },
    "sessionId": {
      "type": "string"
    }
  },
  "required": [
    "afterSequence",
    "sessionId"
  ],
  "title": "ReplaySessionEventsParams",
  "type": "object"
}
