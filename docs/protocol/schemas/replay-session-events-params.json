{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "afterSequence": {
      "description": "The last durable sequence the client has seen; events strictly after this cursor are returned in order, with no gaps and no duplicates. An unreadable stored row returns an `entry.invalid` event at that row's original sequence. Delivering it advances the cursor normally; its data contains `entryId`, `originalKind`, `sequence`, and a safe `reason`.",
      "format": "int64",
      "minimum": 0.0,
      "type": "integer"
    },
    "limit": {
      "description": "Maximum number of events to return. Omitted requests use 500; the server rejects values outside 1..=1000.",
      "format": "uint32",
      "maximum": 1000.0,
      "minimum": 1.0,
      "type": [
        "integer",
        "null"
      ]
    },
    "sessionId": {
      "type": "string"
    },
    "tail": {
      "description": "Return the newest `limit` durable events, still ordered oldest to newest. Intended for bounded activity surfaces, not cursor recovery.",
      "type": [
        "boolean",
        "null"
      ]
    }
  },
  "required": [
    "afterSequence",
    "sessionId"
  ],
  "title": "ReplaySessionEventsParams",
  "type": "object"
}
