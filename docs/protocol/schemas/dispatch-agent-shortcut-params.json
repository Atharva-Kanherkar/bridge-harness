{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "`sessions/dispatch_agent_shortcut` accepts only identity and user intent. Role, model, effort, write scope, and policy are deliberately absent: the host re-resolves all of them from persisted configuration.",
  "properties": {
    "objective": {
      "type": "string"
    },
    "sessionId": {
      "type": "string"
    },
    "token": {
      "type": "string"
    }
  },
  "required": [
    "objective",
    "sessionId",
    "token"
  ],
  "title": "DispatchAgentShortcutParams",
  "type": "object"
}
