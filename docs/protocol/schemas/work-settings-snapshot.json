{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "Effort": {
      "description": "A reasoning-effort level on the wire. Mirrors `bridge_core::delegation::Effort` variant for variant; a mirror test in bridge-core keeps the two from drifting.",
      "enum": [
        "low",
        "medium",
        "high",
        "xhigh"
      ],
      "type": "string"
    },
    "HarnessId": {
      "description": "Which agent runs a session, e.g. 'claude', 'codex', 'gemini'. Identifies the agent only — how Bridge runs it is not encoded here.",
      "pattern": "^[a-z0-9][a-z0-9._-]{0,63}$",
      "type": "string"
    },
    "WorkBriefLimits": {
      "additionalProperties": false,
      "description": "Hard bounds on a briefing run. Reaching one terminates the run; a preflight estimate over one refuses it.",
      "properties": {
        "costCeilingMicrousd": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "maxOutputTokens": {
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "maxToolCalls": {
          "format": "int64",
          "type": "integer"
        },
        "maxTurns": {
          "format": "int64",
          "type": "integer"
        },
        "maxWallSeconds": {
          "format": "int64",
          "type": "integer"
        }
      },
      "required": [
        "maxToolCalls",
        "maxTurns",
        "maxWallSeconds"
      ],
      "type": "object"
    },
    "WorkBriefingProfile": {
      "additionalProperties": false,
      "description": "The briefing model, pinned explicitly. Not a `ProfilePurpose`: profile validation wants exactly one profile per purpose, and briefing permits no fallback at all.",
      "properties": {
        "effort": {
          "anyOf": [
            {
              "$ref": "#/definitions/Effort"
            },
            {
              "type": "null"
            }
          ]
        },
        "harness": {
          "$ref": "#/definitions/HarnessId"
        },
        "model": {
          "type": "string"
        }
      },
      "required": [
        "harness",
        "model"
      ],
      "type": "object"
    },
    "WorkSettings": {
      "additionalProperties": false,
      "description": "Work's configuration.",
      "properties": {
        "briefing": {
          "anyOf": [
            {
              "$ref": "#/definitions/WorkBriefingProfile"
            },
            {
              "type": "null"
            }
          ],
          "description": "`None` means Work is facts-only. There is no implicit model."
        },
        "cooldownMinutes": {
          "format": "int64",
          "type": "integer"
        },
        "enabledConnectorInstances": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "includeReadMentions": {
          "default": false,
          "description": "Include threads that mention the user even when they are already read.\n\nOff by default because the board is about what still needs attention, and a read thread usually does not. On, it is the setting that makes the pipe testable: a mention you have already seen is a signal you can produce on demand, where genuinely new activity is not.\n\n`default` because `WorkSettings` denies unknown fields, so a settings row written before this field existed has to keep deserializing.",
          "type": "boolean"
        },
        "limits": {
          "$ref": "#/definitions/WorkBriefLimits"
        },
        "refreshIntervalMinutes": {
          "description": "`None` disables cadence refresh; manual still works.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "refreshOnFocus": {
          "type": "boolean"
        }
      },
      "required": [
        "cooldownMinutes",
        "enabledConnectorInstances",
        "limits",
        "refreshOnFocus"
      ],
      "type": "object"
    }
  },
  "description": "`work/read_settings` and `work/write_settings` result.\n\n`configured` is the row's existence, not its contents: a fresh install reads defaults with `configured: false`, while a user who wrote settings with no briefing profile reads `configured: true` — briefing explicitly off, which is a different stored state from never having set it up.",
  "properties": {
    "configured": {
      "type": "boolean"
    },
    "settings": {
      "$ref": "#/definitions/WorkSettings"
    }
  },
  "required": [
    "configured",
    "settings"
  ],
  "title": "WorkSettingsSnapshot",
  "type": "object"
}
