{
  "$comment": "GENERATED by bridge-protocol — do not edit. Regenerate: cargo run --manifest-path src-tauri/Cargo.toml -p bridge-protocol --bin generate-protocol-artifacts",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "The inbox's own preferences.",
  "properties": {
    "includeReadMentions": {
      "description": "Stop read state excluding an item, so a mention already opened in the provider's own app still arrives here once.",
      "type": "boolean"
    }
  },
  "required": [
    "includeReadMentions"
  ],
  "title": "ConnectorSetSettingsParams",
  "type": "object"
}
